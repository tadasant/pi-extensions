# Dependencies
node_modules/
.pnp
.pnp.js
.npm
.npm-cache/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Test / coverage artifacts
coverage/
.nyc_output/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# e2e fixtures: the pinned Pi CLI is downloaded, never committed
.e2e-cache/

# Pi's per-project install dirs are generated; .pi/settings.json and .pi/extensions/
# are meant to be committed and shared, so do NOT ignore .pi/ wholesale.
.pi/npm/
.pi/git/
.pi/trust.json

# Environment
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Agent harness runtime files injected per session (never committed)
.claude/skills/
.mcp.json
